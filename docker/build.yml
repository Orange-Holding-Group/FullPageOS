version: '3.6'

services:
  custompios:
    image: ghcr.io/guysoft/custompios:devel
    container_name: magnet-monitor-build
    tty: true
    restart: always
    privileged: true
    volumes:
      - ./:/os_make
    devices:
      - /dev/loop-control